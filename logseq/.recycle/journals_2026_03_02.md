- [[android notes]]
	- [[any { predicate }]] #predicate
		- returns `true` if at least one element matches the condition
		- example
			- ```kotlin
			  val list = lisfOf(1, 2, 3, 4, 5)
			  val hasEven = list.any { it % 2 == 0 } // returen true
			  ```
	- [[none { predicate }]] #predicate
		- retuns `true` if no elements match the condition
		- example
			- ```kotlin
			  val list = listOf(1, 3, 5)
			  val noEven = list.none { it % 2 == true } // returns true
			  ```
	- [[all { predicate }]] #predicate
		- returns `true` if all elements match the condition
		- example
			- ```kotlin
			  val list = listOf(2, 4, 6)
			  val allEven = list.all { it % 2 == 0 } // return true
			  ```
	-
	-